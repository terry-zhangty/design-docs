# AGENTS.md

Static HTML design documentation site. No build system, no dependencies — files are served directly or opened in a browser.

### Local preview

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080`. The site consists of standalone HTML pages in subdirectories, each with its own `index.html`.
